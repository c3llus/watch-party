# Self-Hosted Deployment Environment Example
# Copy this to .env for self-hosted deployment

# Option 1: Let the system auto-detect your domain (recommended)
VITE_MODE=production-selfhost

# Option 2: Override with custom URLs (if needed)
# VITE_API_URL=https://your-domain.com:8080
# VITE_WS_URL=wss://your-domain.com:8081

# Option 3: For development/testing on custom network
# VITE_API_URL=http://192.168.1.100:8080
# VITE_WS_URL=ws://192.168.1.100:8081
